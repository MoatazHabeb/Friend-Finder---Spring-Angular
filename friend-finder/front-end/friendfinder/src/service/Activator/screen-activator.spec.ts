import { ScreenActivator } from './screen-activator';

describe('ScreenActivator', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new ScreenActivator()).toBeTruthy();
  });
});
